{
  "name": "citifix-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node -e \"const fs=require('fs'); if(fs.existsSync('frontend/dist')) { fs.cpSync('frontend/dist', 'dist', {recursive: true}); }\"",
    "build:frontend": "npm --prefix frontend run build",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend start",
    "install:all": "npm --prefix frontend install; npm --prefix backend install"
  }
}
